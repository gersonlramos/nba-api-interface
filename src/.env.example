# =================================
# NBA API - Environment Variables
# =================================

# Server Configuration
PORT=3000                    # Port for the server to run on
HOST=localhost              # Host address (localhost for development)
NODE_ENV=development        # Environment: development, production

# API Configuration (REQUIRED)
RAPID_API_KEY=your_rapidapi_key_here    # Get from: https://rapidapi.com/api-sports/api/api-nba
EC2_URL=http://your-ec2-instance-url/   # Your AWS EC2 deployment URL (optional)

# CORS Configuration
CORS_ORIGIN=*               # Allowed origins (* for development, specific URLs for production)

# Logging Configuration
LOG_LEVEL=info              # Logging level: error, warn, info, debug

# =================================
# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Replace 'your_rapidapi_key_here' with your actual RapidAPI key
# 3. Update EC2_URL if you have a deployment URL
# 4. Never commit the .env file to version control
# ================================="